#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 1 ]]; then
  echo "Usage: agent_scripts/next_slice <task_id>" >&2
  exit 1
fi

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
task_id="$1"
"$script_dir/report_state" "$task_id" next_slice "Ready for next slice."
